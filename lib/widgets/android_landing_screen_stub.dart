class AndroidLandingScreenHelper {
  static void continueOnWeb() {
    // Stub implementation for non-web platforms
    // This should never be called on mobile
    throw UnsupportedError('continueOnWeb is only available on web platform');
  }
}
